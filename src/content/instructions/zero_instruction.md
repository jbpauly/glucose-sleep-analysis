2. Export Zero Fasting Log and Save as a CSV
    - Me (lower right) -> Settings (upper right) -> My Data -> Export Data 
    - **Check if file exports as a TXT or CSV file and re-save as a CSV if necessary**